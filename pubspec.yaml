name: remove_accentuation_extension
description: Remove accents, in a simple way quickly using the extension.
version: 0.0.2
homepage: https://github.com/matheusperez/remove_accentuation_extension

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
